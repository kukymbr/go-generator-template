package example

// Added as an example usage.
// To regenerate example files in the {{ cookiecutter.__project_name }} repository, use `make generate_example`.
//go:generate go tool {{ cookiecutter.__project_name }} --package=example
